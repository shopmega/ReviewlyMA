'use client';

interface CompetitorAdsProps {
  businessId: string;
  trackingEnabled?: boolean;
  className?: string;
}

export default function CompetitorAds(_props: CompetitorAdsProps) {
  // Competitor ads module decommissioned.
  return null;
}
